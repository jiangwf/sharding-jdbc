<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.dangdang.sharding.unit.dao.UserDao">
    <resultMap id="resultMap" type="com.dangdang.sharding.unit.model.User" extends="com.dangdang.sharding.unit.dao.UserMapper.BaseResultMap">

    </resultMap>

    <sql id="Base_Column_List" >
        <!--
          WARNING - @mbggenerated
          This element is automatically generated by MyBatis Generator, do not modify.
          This element was generated on Fri Jun 22 17:44:26 CST 2018.
        -->
        id, name, age, remark, company_code, create_time
    </sql>

    <update id="updateList" parameterType="com.dangdang.sharding.unit.model.User">
        UPDATE user SET age=#{age} where id=#{id}
    </update>

    <select id="selectForceIndex" parameterType="com.dangdang.sharding.unit.model.User" resultType="com.dangdang.sharding.unit.model.User">
        select * from user FORCE INDEX(index_name) where name = #{name};
    </select>

    <update id="updateForceIndex" parameterType="com.dangdang.sharding.unit.model.User">
        update user FORCE INDEX(index_name) set age=#{age} where name = #{name};
    </update>

    <update id="updateUser" parameterType="com.dangdang.sharding.unit.model.User">
        update user set age=#{age} where name = #{name};
    </update>

    <select id="selectList" parameterType="com.dangdang.sharding.unit.model.User">
      select t.* from user t,user_info f where t.id = f.user_id and f.user_id= #{id}
    </select>
</mapper>